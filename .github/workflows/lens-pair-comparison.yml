name: Lens comparison on two new original photos

on:
  push:
    branches: ["experiment/searchapi-lens-20260909"]
    paths:
      - ".github/workflows/lens-pair-comparison.yml"
      - "tools/lens_pair_comparison.py"
      - "tools/regression/fixtures/lens-pair-20260909/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lens-pair-comparison
  cancel-in-progress: false

jobs:
  compare:
    strategy:
      fail-fast: false
      matrix:
        provider: [searchapi, oxylabs]
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Search both original images without text hints
        env:
          SEARCHAPI_API_KEY: ${{ secrets.SEARCHAPI_API_KEY }}
          OXYLABS_USERNAME: ${{ secrets.OXYLABS_USERNAME }}
          OXYLABS_PASSWORD: ${{ secrets.OXYLABS_PASSWORD }}
        run: python3 tools/lens_pair_comparison.py "${{ matrix.provider }}"
      - name: Save sanitized results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lens-pair-${{ matrix.provider }}-${{ github.run_id }}
          path: pair-lens-results/${{ matrix.provider }}/
          if-no-files-found: warn
          retention-days: 7
