# Build 156: photo reading and slab set/year correction

The build155 phone recording read the grading label but rejected all catalog matches because `slabSetName=2002 EXPEDITION` was compared with `Expedition Base Set`. Build155 only split a generic `SET_AND_YEAR_LABEL`, missing explicitly typed slab set names. Normalize the leading year/season into a linked slabYear atom and the remainder into slabSetName, retaining original text. No card-specific lookup table or candidate override is added.

The user supplied FlipCheck_Beta_v0.26.apk (SHA256 6803ab0bc086c48d26672311b519024e489f3010894a86b8234b301abd569d3c). Its assets/index.html contains the observer and resolver source. It uses one Vision request, then zero or one optional Web request if shouldResolveOnline returns true; it uses the same configured model name as the current project. The observer requests literal discriminating labels and grading data. The old UI also accepts model-provided confidence and readiness before a catalog check. Static inspection explains fewer sequential requests but does not establish superior accuracy or measured latency.

Build156 reuses the literal-first inventory approach with the current located evidence schema. LEGGI FOTO runs one Vision request with no automatic retry or Web search. Its PHOTO_READ result displays observations including slab fields; it does not claim catalog confirmation or make the item market-ready. VERIFICA COMPLETA preserves the existing Vision -> Web -> Vision -> Web route. When an eligible saved photo reading exists and the image-content and user-hint digest matches, verification resumes from that response with only three new calls. Reported total usage includes the already-paid first stage once. Different photos/hints and incomplete/failed readings cannot be reused.

The foreground service owns both routes and persists their results. Diagnostics distinguish photoReadingOnly and include the readable summary; they omit the input digest. Tests exercise the production orchestrator using both recorded Vision payloads, both recorded Web payloads, all competing candidates, and explicit simulated tool-source bindings. No live API or on-device speed/accuracy measurement is part of this build.
