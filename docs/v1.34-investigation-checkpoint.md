# v1.34 investigation checkpoint — NOT A RELEASE

## Verified baseline

The available delivered v1.33 APK has SHA-256
`4e6dc98e8598aa3762bd6f559d84e5060b0ede9382bd349d7b421d5d341182b0`.
Its CI archive records commit `354734d583b5388c99cb42950e143a6e232df466`,
versionCode 137 and versionName `1.33-live-validated-global-resolver`.
The local equivalent source commit is `d4942f7`; remote/local tree equality
was established during the delivery. This does not independently verify
which binary is currently installed on the user's phone.

Engine: UniversalIdentityEngineV2; reducer: FinalStateReducerV2.
Production model: gpt-5.6-luna. Prompts, schemas, API configuration,
application ID, signing and version metadata are unchanged at this checkpoint.
Baseline source hashes (SHA-256):

- OpenAiClient.java: 9116884aa1732fdfa6a43b1b19b346ae46184dcaaf880bdb1fb57c70e7aefbd9
- UniversalIdentityEngineV2.java: 55a29e9f194fb9a7aed0df09d660fbae4b89cabf3fbe5a7f810d92695a74c146

Comparison to local v1.32 `8c27865` covers 31 changed files. The reviewed
v1.33 extractor introduces mandatory OCR corroboration for identity labels;
this can demote a localized Vision transcription. It has NOT been proven
to be the cause of the user's particular sealed-box output.

## Defects reproduced before production edits

| Contract | Baseline failure | Corrected result |
|---|---|---|
| Normalize derived denominator repeatedly | 64 becomes 15/64 | remains 64; ledger size stable |
| Non-holo finish | NON HOLO becomes HOLO | NON_HOLO |
| Collector total mapping | collector_total becomes collectorNumber | printedTotal |
| Jersey/graphic distinction | jersey_number becomes graphicNumber | jerseyNumber |

Root causes are respectively reprocessing immutable raw text on derived
atoms, positive substring matching before negation, an overly broad
`contains("collector")` profile rule, and a shared alias.
These are code-level reproductions, not live image reproductions.

Tests: V134EvidenceIntegrityTest initially 4 failures / 4; after edits 4/4
PASS. Existing v1.32 deterministic replay: 10/10 PASS; existing v1.33
deterministic replay: 15/15 PASS. Fresh Java compilation used current core
sources with the previously available JVM image-preparation substitute.
No Android preprocessing or OCR execution is claimed by these tests.

## Open investigation

- LocalVisionEngine merges OCR text from several rotations without keeping
  line bounding boxes in LocalScan. Causal link to #18 is unproven.
- Focused image selection assumes sports back is index 1; crop provenance
  is trace text rather than an index-remapping contract. Order invariance
  needs implementation and actual Android tests.
- CanonicalFieldKey and the V2 vocabulary differ. The presentation bridge,
  unknown-field retention and exact live rejection trace still need review.
- Hobby configuration resolution, brand/manufacturer versus compatibility,
  semantic comparison and calibrated evidence presentation are not completed.
- Do not claim any of the four user-reported cases fixed live yet. The latest
  transcription documents three v1.33 cases and the remote control in v1.32.

## Release blockers and next steps

Original supplied fixture files remain available under
`tools/regression/fixtures/v132-real-images`. Kobe inputs are screenshots,
not camera originals. Their original user-supplied screenshot UI must be
included in contamination tests. Actual v1.33 failing OCR/Vision/Web traces
have not been supplied here; existing recorded replays are not those runs.

No adb or Android SDK is available locally. No installed-APK tests, live
API calls, new screenshots, live cost measurements or new APK were produced.
Do not promote this checkpoint. Continue diagnosis on the actual failing
traces, add non-development cases, implement remaining corrections, then
run the exact candidate APK twelve times with production services.

Proposed release is v1.34 / versionCode 138, subject to checking the remote
version inventory before assignment. This checkpoint deliberately retains
137 so the existing metadata regression checks remain valid; it is not a
v1.34 binary. New local branch: codex/v1.34-evidence-integrity.
No remote push or publication is authorized for this branch yet.

## Continuation 2026-09-04 (after 79aade8)

The newly attached five inputs match the existing test fixtures byte-for-byte.
This establishes input-file identity, not reproduction of previous API responses.
The two card views below are screenshots including external UI, not camera originals.

| Test input | SHA-256 |
|---|---|
| Remote front | bd26e4a6b2bca3348fef87914c4bda6a89a1232209d7cc9103b0e3e42c3b108c |
| Box front | 205f9daef98308b6dc2d85a472424f818a5ae9d99b47e080211e26dc5f0ea883 |
| Sports front screenshot | 54f16d9e5c6a888df7d6ad0612a9e3c96a9751d03d85d0d76ab2311d4484eb82 |
| Sports back screenshot | 717ab1814def0ce5bb3930db7b74f0816d97181cd11ef6e44e40b0ee94a712b1 |
| TCG front | 25cd597509a77517eb163d752d1866203d14c571650e85d3c98122da3b0fb2ec |

Four additional contract tests were added and executed BEFORE correction:
all four failed, while the original four passed.

| Confirmed code defect | Correction |
|---|---|
| Camel-case fields such as cardRole and subSeries lose case on canonicalization | Split camel-case boundaries before key normalization; preserve unknown descriptive keys and register domain aliases |
| Canonical productReleaseYear/setSeason bypass statistical-scope routing | Route these year aliases to statisticsSeason when explicitly scoped as statistics |
| Evolution stage has no distinct canonical field | Add evolutionStage; keep catalog cardRole separate, including explicitly evolution-scoped input |
| Shared tokens make sibling product lines compatible | Require containment/equivalence for hierarchical matching, not partial token overlap; generic product type remains compatible with its box subtype |

Final local checks: contract tests 8/8; v1.32 replay 10/10; v1.33 replay 15/15.
These compile current core Java against the existing JVM substitutes. They
do not execute Android OCR, image preparation, networking or the installed APK.
The old replay fixtures include example URLs; they are NOT original live API
traces and the new user transcription was not converted into a replay.

Environment check: adb/emulator/sdkmanager not on PATH and not found in the
checked /opt, /usr/local and /workspace locations (depth 5); /dev/kvm absent.
No Android execution tool is exposed. The existing remote live workflow
requires signing secrets and FLIPCHECK_LIVE_API_KEY; their availability has
not been verified in this continuation. No remote workflow was triggered.

Still not completed: image-region/rotation integrity, focused crop index
remapping, production-to-UI mapping audit, credential-safe diagnostic export,
live brand/layout verification, unique commercial-format resolution and
three uncached installed-APK scans per case. In particular the cause of #18
remains unproven. No new APK, live trace, screenshot or measured API cost was
produced. API calls for recognition in this continuation: zero.

Next execution step requires an Android-capable environment. The existing
GitHub runner is a possible route after explicit authorization to push this
v1.34 branch and verification of its configured live-test credentials. Keep
the release gate blocked; do not label these changes a validated release.


## Build 142: transport diagnosis after build 141

Run 33963545473 / source d6f5ba2e1f106bb40fb9aeb71147594b025f1335
completed with 0/12: all scans returned empty identity and VISION_TECHNICAL,
no stage payloads and zero recorded usage. Actual billing is unknown.
APK SHA256 aa3416d3bd40d98db9260b527b2cb85e6d33bd28817eefb4e35b4cd4d855014e verified.
The old client discarded HTTP status/code, then the engine discarded the
exception message. The underlying live cause cannot be recovered from this artifact.

Build 142 preserves allowlisted API error codes and HTTP status, including
non-JSON error bodies, without retaining messages/body/credentials. Permanent
HTTP failures (including insufficient_quota) no longer trigger an unchanged
retry. Temporary failures retain at most the existing single retry; attempted
retries are counted even when they fail. Primary and retry reasons are exported.
This repairs error handling and observability, not a proven repair of the
unobserved API cause. The next live run must establish that cause or verify
recovery. Identity thresholds and 12/12 criteria remain unchanged.
Local validation: 62 JUnit contracts and replay 10/10, 15/15; JVM transport stub,
not live API/Android validation. Android build and production client compilation
remain gated in CI. Build metadata is 142 / 1.34-evidence-integrity.


## Build 143: distinguish unknown 429 and respect retry headers
Build 142 run 33964920280: 12/12 fail HTTP 429, code unspecified on primary
and retry. APK SHA256 77d82eb73e805214ed840db7fec073e4f8a265388b1e3bf97106c8eb84853afe verified.
No proof of exhausted credit: old mapping called all unspecified 429 rate limit.
Build 143 classifies allowlisted error.type or known quota/rate-limit phrases
when code is absent, retaining no free text. Unknown 429 remains unclassified.
Retry-After seconds/date respected up to 30 seconds; longer delays defer instead
of retrying early. Suite stops after shared API failure; incomplete count fails
the unchanged 12/12 gate. No false successful scans or skipped-as-pass entries.
Three pre-fix diagnostic tests failed, now pass. Local 68 contracts and replay
10/10,15/15 passed (JVM transport stub, no live validation).
