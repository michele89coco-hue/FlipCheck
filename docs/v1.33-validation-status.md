# FlipCheck v1.33 — validation status

This file deliberately separates checks executed in the current workspace from checks that require Android/API infrastructure. A candidate must not be described as live validated while any mandatory live gate remains unavailable.

## Executed locally

| Check | Result | Evidence |
|---|---|---|
| Java compilation of the V2/V3 resolver core | PASS | Production models, ledger, normalizer, retriever, verifier, conflict resolver and final reducer compiled with Java 17 |
| v1.32 deterministic replay preservation | PASS | `V132UniversalIdentityEngineV2RegressionTest: PASS (10/10)` |
| v1.33 isolation/grounding/reducer suite | PASS | `V133LiveValidatedGlobalResolverRegressionTest: PASS (15/15)` |
| Gradle/JUnit bridge | PASS | `V133ReleaseGateJUnitTest: OK (1 test)` |
| Production hardcode scan | PASS | Main sources contain none of the four primary fixture identities |
| Fail-closed release gate negative test | PASS | Missing live report returns exit 1 and `status=FAIL` |
| Source whitespace/error check | PASS | `git diff --check` |

The deterministic replay uses the original image files for integrity/metamorphic checks and recorded production-shaped payloads for reducer stability. It is not counted as a live API test.

## Mandatory checks not executed in this workspace

| Check | Status | Blocker |
|---|---|---|
| Android Gradle build/lint | NOT RUN | Gradle 9.1.0 is present, but Android Gradle Plugin 9.0.1 and Android SDK 36 are absent from the local cache and cannot be fetched through the restricted network |
| APK v1.33 generation | NOT RUN | Same AGP/Android SDK blocker |
| Instrumented test APK | NOT RUN | No local Android SDK |
| Emulator install and launch | NOT RUN | `adb`, emulator and AVD tools are absent |
| Live production Vision/Web | NOT RUN | No FlipCheck/OpenAI API credential is exposed to this workspace |
| Twelve live real-image runs | NOT RUN | Requires installed APK, emulator/device and live credential |
| Samsung S24 Ultra | NOT RUN | No physical device is connected to this workspace |

The branch therefore contains a live-testable implementation and automated gate, but it is not yet a live-validated release. The ordinary CI workflow labels its artifact as a **candidate**. Only `.github/workflows/android-v133-live-release-gate.yml` may produce a passing live gate, and it requires the `FLIPCHECK_LIVE_API_KEY` and existing compatible-signing secrets.

## Deterministic before/after

These are reducer/replay outcomes, not live-API claims.

| Case | v1.32 live failure reported by device | v1.33 deterministic expected state |
|---|---|---|
| Sealed box | partial title, official candidates rejected, no winner | full 2025-26 manufacturer/family/subseries, core confirmed, format/SKU pending, lookalike configuration rejected |
| Sports card | season truncated; unrelated checklist numbers mixed | full 1997-98 season and physical/catalog #81 agreement; other rows rejected |
| TCG | incompatible set confirmed; visible edition lost | exact Jungle #15/64 tuple; First Edition/Holo retained; generic incompatible set page rejected |
| TV remote | inferred Samsung anchored queries/title | neutral query first; inferred brand excluded from title; strong Philips layout candidate required; rear model photo remains specific |

## Cost status

Replay accounting remains within USD 0.025 per case. No new live cost is claimed. The installed-APK suite fails any live run above USD 0.025 and records every Vision/Web request. A neutral-query retry is permitted only when the recorded estimated total remains within the budget.

## Promotion procedure

1. Push the v1.33 branch only after explicit authorization.
2. Configure `FLIPCHECK_LIVE_API_KEY` together with the existing compatible-signing secrets.
3. Run **FlipCheck v1.33 installed APK live release gate** on the exact commit.
4. Download its artifact only if `v133-release-gate.json` says `PASS`.
5. Verify that the artifact APK SHA-256 equals the SHA recorded by every live run.
6. Perform the final Samsung S24 Ultra verification separately and report it as a real-device test.
