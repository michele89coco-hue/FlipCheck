# FlipCheck v1.32 — Validation status

This document separates executed checks from checks that require infrastructure not present in the local workspace.

## Four attached regressions

The five original JPEG files are stored under `tools/regression/fixtures/v132-real-images/` and are verified by size, decodability, dimensions, and SHA-256 before deterministic replay.

| Case | v1.31 failure | v1.32 deterministic replay result | Cost represented by replay |
|---|---|---|---:|
| Topps sealed box | Upper Deck inference promoted to photo fact; false conflict | Topps remains observed/retrieved, Upper Deck remains inferred, core `CONFIRMED`, format/SKU pending | USD 0.015 |
| Kobe front + back | `#81` missing; statistics season contaminates product year | `#81` photo and catalog atoms agree; `1997-98` product year; `1996-97` statistics only; core `CONFIRMED` | USD 0.015 |
| Vileplume | `I5/64` versus `15/64`; ambiguous edition text creates conflict | collector `15/64`; no conflict; First Edition and Holo retained; core `CONFIRMED` | USD 0.015 |
| Philips remote | no brand, no Web recovery | remote profile; Philips catalog candidate wins layout disproof; core `CONFIRMED`; exact model pending; rear-label request is explicit | USD 0.015 |

The replay test exercises the real reducer and fixture files with version-controlled, production-shaped OCR/Vision/Web payloads derived from the visible evidence and the reported v1.31 failures. These payloads are deterministic regression inputs, not a claim that an API call was made from the local workstation.

## Executed locally

- Java compilation of the v1.32 core against the production model classes: passed.
- Deterministic replay and source invariants: `10/10` passed.
- Real-image integrity and decoding: passed for all five originals.
- Metamorphic resize, compression-equivalent brightness change, two-degree rotation, and center crop: passed for the four identities.
- Production source scan for object-specific hardcode: passed.
- Preservation scan for card serial, First Edition, Shadowless, barcode, camera, and background-service axes: passed.

## Not executed locally

- Android Gradle unit/lint/build: blocked because the Android Gradle Plugin and Android SDK dependencies are not available in the local offline cache.
- Instrumented Android tests: not run.
- Emulator installation/launch: not run.
- Samsung S24 Ultra/device test: not run.
- Live API multimodal run: not run; no configured FlipCheck API credential is exposed to this workspace.
- The requested 45-object global real-image matrix: not complete. Only the five user-supplied originals for the four primary regressions are available. No synthetic or unrelated screenshot has been relabeled as a real-object fixture.

The canonical GitHub workflow is configured to run Gradle unit tests, lint, assemble the signed-compatible APK, verify its certificate and manifest, and publish `FlipCheck-v1.32.apk` after the v1.32 branch is explicitly authorized for remote push.
